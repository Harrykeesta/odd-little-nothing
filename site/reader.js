// Links also work without JavaScript. This adds keyboard and touch navigation.
document.addEventListener('keydown',e=>{if(e.altKey||e.ctrlKey||e.metaKey||/INPUT|TEXTAREA|SELECT/.test(e.target.tagName))return;const rel=e.key==='ArrowLeft'?'prev':e.key==='ArrowRight'?'next':null;const link=rel&&document.querySelector(`a[rel="${rel}"]`);if(link){e.preventDefault();location.href=link.href}});
let start;const strip=document.querySelector('figure');strip?.addEventListener('touchstart',e=>{if(e.touches.length===1)start=[e.touches[0].clientX,e.touches[0].clientY];else start=null},{passive:true});strip?.addEventListener('touchend',e=>{if(!start)return;const dx=e.changedTouches[0].clientX-start[0],dy=e.changedTouches[0].clientY-start[1];start=null;if(Math.abs(dx)>70&&Math.abs(dx)>Math.abs(dy)*2){const a=document.querySelector(`a[rel="${dx>0?'prev':'next'}"]`);if(a)location.href=a.href}},{passive:true});
